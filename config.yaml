# Multiple Server Configuration
servers:
  - name: "Default Server"
    host: "127.0.0.1"
    port: 10011
    username: "serveradmin"
    password: ""

# Global settings
metrics_port: 8000
read_interval: 60
